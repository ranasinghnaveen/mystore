<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asssss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Asssss">
        <activity
            android:name=".Main_Screen"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/Theme.Asssss.NoActionBar"></activity>
        <activity android:name=".Confirm_Password" />
        <activity android:name=".Enter_otp" />
        <activity android:name=".Reset_password" />
        <activity android:name=".signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>